import { imageController } from './controller/image.controller';
import { categoryController } from './controller/category.controller';

export const galleryControllers = [imageController, categoryController];
